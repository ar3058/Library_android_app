<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.example.library">

    <application
        android:allowBackup="true"
        android:icon="@drawable/lib"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lib"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".exp3"></activity>
        <activity android:name=".exp2" />
        <activity android:name=".cp_exp" />
        <activity android:name=".cp" />
        <activity android:name=".home" />
        <activity android:name=".guest" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".login" />
    </application>

</manifest>